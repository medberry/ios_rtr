---
- name: List packages and report
  hosts: all
  gather_facts: yes
  become: yes

  tasks:
  - name: Generate RPM list
    yum:
      list: installed
    register: pkg_list

  - name: Report RPM list
    debug:
      var: "{{ item }}"
    with_items: pkg_list.results